# Changelog

## 1.0.4

- 将包描述调整为中文，便于在 ohpm 三方库页面展示。

## 1.0.3

- 补充发布评分所需的 `homepage`、`repository` 元数据。
- 新增 `example` 示例目录，便于三方库平台识别示例文档。

## 1.0.2

- 更新 README 效果预览为 CDN GIF 链接，提升 ohpm 仓库页面展示稳定性。
- 补充 `homepage`、`repository` 元数据，并新增 `example` 示例目录。

## 1.0.1

- 将包版本升级到 `1.0.1`，用于发布个人包名版本。
- 补充 README GIF 效果预览，展示抽屉收起、拖拽中、展开三种状态。
- 补充 `drawerWidth` 抽屉宽度配置，并完善高度/宽度配置文档。
- 将兼容性说明统一为 HarmonyOS API 12 及以上，与当前 HAR 发布元数据保持一致。
- 将安装、导入和使用说明统一为个人包名 `workbench-drawer`。

## 1.0.0

- 初始版本。
- 支持默认标题区域与默认内容区域。
- 支持 `titleBuilder` 自定义标题区域。
- 支持 `contentBuilder` 自定义内容区域。
- 支持类似 iOS 底部抽屉的上滑展开、下滑收起、方向吸附、半高吸附。
- 支持遮罩展示与点击遮罩收起。

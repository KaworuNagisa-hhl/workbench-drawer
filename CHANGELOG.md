# Changelog

## 1.0.6

- 优化 like-ios 底部抽屉的上拉、下拉和吸附过程，避免拖拽期间外层尺寸变化反复重置抽屉高度导致卡顿或动画跳动。
- 将拖拽手势收敛到标题/手柄区域，内容区默认保留给按钮、列表、横向滑动和业务交互，减少对主页面滚动的影响。
- 修复 `HitTestMode.Block` 阻断子节点命中导致标题点击、内容按钮点击不稳定的问题。
- 折叠态只渲染标题区域，避免收起时内容区节点泄漏或继续覆盖页面。
- 保持抽屉默认浅色毛玻璃外观，不跟随其它 like-ios 组件切换为黑色优先默认风格，避免破坏已接入业务的视觉预期。
- 将 HAR 模块 `deviceTypes` 调整为当前 OpenHarmony SDK 推荐的 `default`，减少发布/编译阶段的设备类型警告。
- 保留 `enableContentDrag` 配置能力：默认关闭内容区拖拽以保护列表、按钮、横向滑动等子组件交互，业务需要时仍可主动开启。
- 更新发布描述和关键词，强调 like-ios、frosted-glass、bottom-sheet 组件定位。

## 1.0.5

- 优化抽屉拖拽手势判定：只有当纵向位移超过阈值且明显大于横向位移时，才由抽屉接管手势。
- 修复内容区存在横向滚动内容时，抽屉过早拦截 `TouchEvent` 导致子组件无法左右滑动的问题。
- 修复抽屉内容区子组件在 `Down` 阶段被过早 `stopPropagation()`，从而影响点击/滚动响应的问题。
- 补充 `DrawerMath.shouldCaptureVerticalDrag()` 及对应单测，保证拖拽方向判定逻辑可回归。

## 1.0.4

- 将包描述调整为中文，便于在 ohpm 三方库页面展示。

## 1.0.3

- 补充发布评分所需的 `homepage`、`repository` 元数据。
- 新增 `example` 示例目录，便于三方库平台识别示例文档。

## 1.0.2

- 更新 README 效果预览为 CDN GIF 链接，提升 ohpm 仓库页面展示稳定性。
- 补充 `homepage`、`repository` 元数据，并新增 `example` 示例目录。

## 1.0.1

- 将包版本升级到 `1.0.1`，用于发布个人包名版本。
- 补充 README GIF 效果预览，展示抽屉收起、拖拽中、展开三种状态。
- 补充 `drawerWidth` 抽屉宽度配置，并完善高度/宽度配置文档。
- 将兼容性说明统一为 HarmonyOS API 12 及以上，与当前 HAR 发布元数据保持一致。
- 将安装、导入和使用说明统一为个人包名 `workbench-drawer`。

## 1.0.0

- 初始版本。
- 支持默认标题区域与默认内容区域。
- 支持 `titleBuilder` 自定义标题区域。
- 支持 `contentBuilder` 自定义内容区域。
- 支持类似 iOS 底部抽屉的上滑展开、下滑收起、方向吸附、半高吸附。
- 支持遮罩展示与点击遮罩收起。

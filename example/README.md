# Example

`WorkbenchDrawerDemo.ets` provides a minimal ArkUI page showing `WorkbenchDrawer` in a `Stack`.

Copy the file into an OpenHarmony/HarmonyOS demo project, then adjust the import path according to your project structure.
